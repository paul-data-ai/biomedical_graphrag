[phases.setup]
nixPkgs = ["python313", "python313Packages.pip"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "python -m pip install -e ."]

[start]
cmd = "uvicorn biomedical_graphrag.api.main:app --host 0.0.0.0 --port $PORT"
